# KodosiQT

Follow `AGENTS.md`. This is a thin Qt Quick presentation adapter over the
authoritative Rust runtime in `../Kodosi`; QML must never become a second
protocol, security, mutation, session, or terminal authority.

Before any UI or visual-system change, read and follow `UI-DONTS.md`. Its
prohibitions are binding design rules, not suggestions.
