Kodosi redistributes the Qt 6.11.2 open-source license texts that apply to
the bundled Qt runtime and the third-party components identified by Qt's
binary-distribution SBOMs.

Kodosi does not claim or rely on a Qt commercial license. The packaged Qt
libraries remain dynamically linked and replaceable inside the private
runtime directory. The authoritative SPDX and CycloneDX documents shipped
with the exact Qt 6.11.2 linux_gcc_64 binary distribution are installed in
the adjacent sbom directory.

Source URLs, refs, archive digests, and individual file digests are recorded
in /usr/share/doc/kodosi/provenance/native-license-evidence.json.
